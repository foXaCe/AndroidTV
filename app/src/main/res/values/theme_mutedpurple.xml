<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="theme_muted_purple_accent">#627EC8</color>
    <color name="light_grey_transparent">#2EAAAAAA</color>

    <style name="Theme.Jellyfin.MutedPurple" parent="Theme.Jellyfin">
        <!-- Colors -->
        <item name="android:colorAccent">@color/theme_muted_purple_accent</item>
        <item name="defaultSearchColor">@color/theme_muted_purple_accent</item>
        <item name="progressPrimary">@color/theme_muted_purple_accent</item>
        <item name="android:colorButtonNormal">@color/light_grey_transparent</item>
        <item name="buttonDefaultNormalBackground">@color/light_grey_transparent</item>
        <item name="buttonDefaultHighlightBackground">@color/theme_muted_purple_accent</item>
        <item name="popupMenuBackground">@color/ds_surface_bright</item>

        <!-- Control icons -->
        <item name="controlIconForegroundActive">@color/theme_muted_purple_accent</item>

        <!-- QR drawables -->
        <item name="qrForeground">@color/theme_muted_purple_accent</item>

        <!-- Rounding Values — aligned with design system -->
        <item name="cardRounding">4dp</item>
        <item name="buttonRounding">10dp</item>
        <item name="inputRounding">10dp</item>
        <item name="popupMenuRounding">10dp</item>

        <!-- Tile colors -->
        <item name="tile_port_person_bg">@color/ds_surface_bright</item>
    </style>
</resources>
